{
  "name": "nestera",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ]
  },
  "dependencies": {
    "@nestjs/swagger": "^11.2.6",
    "@prisma/client": "^7.4.1",
    "prisma": "^7.4.1",
    "swagger-ui-express": "^5.0.1"
  }
}
